import numpy as np
import pandas as pd
columns=['A', 'B', 'C', 'D']
temp_input = []
df_zero = pd.DataFrame(index=columns,columns=columns)
df_zero[:] = 'C, D'
df_zero.A = 'D, D'
print(df_zero)

print(df_zero.groupby('C').count())
